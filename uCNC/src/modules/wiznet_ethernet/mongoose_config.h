#ifndef MONGOOSE_CUSTOM_H
#define MONGOOSE_CUSTOM_H

#include <errno.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
// #include "Ethernet/socket.h"

// #define sockaddr_in freertos_sockaddr
// #define sockaddr freertos_sockaddr
// #define accept(a, b, c) 0
// #define connect(a, b, c) connect((a), (b), (c))
// #define bind(a, b, c) 0
// #define listen(a, b) listen((a))
// #define socket(a, b, c) socket((a), (b), (c), SF_IO_NONBLOCK)
// #define send(a, b, c, d) send((a), (b), (c))
// #define recv(a, b, c, d) recv((a), (b), (c))
// #define setsockopt(a, b, c, d, e) setsockopt((a), (b), (c), (d))
// #define sendto(a, b, c, d, e, f) sendto((a), (b), (c), (e), (f))
// #define recvfrom(a, b, c, d, e, f) \
//   FreeRTOS_recvfrom((a), (b), (c), (d), (e), (f))
// #define closesocket(x) close(x)
// #define gethostbyname(x) -1
// #define getsockname(a, b, c) -1
// #define getpeername(a, b, c) 0

#endif